enum ROLES_ENUM {
  BUTTON = 'button',
  SUBMIT = 'submit',
}

enum TYPES_ENUM {
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  INFORMATION = 'information',
}

const AppButtonProps = {
  ROLES_ENUM,
  TYPES_ENUM,
};

export { AppButtonProps, ROLES_ENUM, TYPES_ENUM };
